pipeline {
    agent any

    stages {
        stage('npm install stage') {
            steps {
                sh '''
                    sh 'npm --version'
                '''
            }
        }
    }
}
